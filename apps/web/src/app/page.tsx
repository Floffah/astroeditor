import { FileUploadForm } from '@/app/FileUploadForm'

export default function Home() {
	return (
		<main className="flex justify-center h-screen w-screen items-center">
			<FileUploadForm />
		</main>
	)
}
